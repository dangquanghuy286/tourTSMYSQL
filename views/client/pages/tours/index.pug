extends ../../layouts/default.pug

block content
    .container.my-3 
        .row 
            .col-12 
                .box-head 
                    h2.inner-title #{title}

        ul 
            each item in tours 
                li #{item.title}